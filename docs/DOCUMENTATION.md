# 📦 Зависимости проекта (Dependencies)

Этот файл содержит список всех зависимостей, используемых в проекте "Совет Элронда", а также их назначение.

## 1. Версия Python и установка
Проект требует Python **3.9.6** или выше.

### Проверка установленной версии:
```sh
python3 --version
```

### Установка Python (если не установлен):
#### macOS:
```sh
brew install python@3.9
```
#### Ubuntu/Debian:
```sh
sudo apt update && sudo apt install python3.9
```
#### Windows:
Скачать и установить с [официального сайта](https://www.python.org/downloads/).

### Обновление Python:
#### macOS:
```sh
brew upgrade python
```
#### Ubuntu/Debian:
```sh
sudo apt update && sudo apt upgrade python3
```
#### Windows:
Удалить старую версию и установить новую с [python.org](https://www.python.org/downloads/).

## 2. Основные зависимости
| Библиотека        | Версия   | Назначение                          | Установка                   |
|-------------------|----------|------------------------------------|-----------------------------|
| fastapi           | 0.115.11 | Веб-фреймворк для API             | `pip install fastapi`       |
| uvicorn           | 0.34.0  | ASGI-сервер для запуска FastAPI    | `pip install uvicorn`       |
| pydantic          | 2.10.6  | Валидация данных                   | `pip install pydantic`      |
| loguru            | 0.7.3   | Логирование                        | `pip install loguru`        |
| python-dotenv     | 1.0.1   | Работа с переменными окружения     | `pip install python-dotenv` |
| PyQt6           | 6.6.1   | Графический интерфейс для тестирования | `pip install PyQt6`   |
| requests        | 2.31.0  | HTTP-клиент для API-запросов           | `pip install requests` |

## 3. Зависимости для тестирования
| Библиотека       | Версия  | Назначение                           | Установка                      |
|-----------------|---------|-------------------------------------|--------------------------------|
| pytest          | 8.3.5   | Фреймворк для тестирования          | `pip install pytest`          |
| httpx           | 0.28.1  | Тестирование API-запросов           | `pip install httpx`           |
| coverage        | 7.4.6   | Подсчет покрытия тестами           | `pip install coverage`        |
| pytest-asyncio  | 0.23.5  | Тестирование асинхронных функций    | `pip install pytest-asyncio`  |
| pytest-mock     | 3.14.0  | Моки для тестирования               | `pip install pytest-mock`     |

## 4. DevOps и утилиты
| Библиотека   | Версия | Назначение                              | Установка                     |
|-------------|--------|----------------------------------------|-------------------------------|
| docker      | 25.0.1 | Контейнеризация приложения            | `brew install docker`         |
| black       | 24.2.0 | Автоформатирование кода               | `pip install black`           |
| isort       | 5.13.2 | Сортировка импортов                   | `pip install isort`           |
| pre-commit  | 3.7.0  | Автоматические хуки для проверки кода   | `pip install pre-commit`      |
| mypy        | 1.9.0  | Статический анализ типов              | `pip install mypy`            |
| flake8      | 7.0.0  | Анализатор стиля кода                  | `pip install flake8`          |

## 5. Зависимости для работы с нейросетями
| Библиотека      | Версия  | Назначение                             | Установка                    |
|----------------|---------|---------------------------------------|------------------------------|
| openai        | 1.10.0  | API-клиент для работы с OpenAI        | `pip install openai`         |
| torch         | 2.2.2   | Поддержка PyTorch для локальных моделей | `pip install torch`         |
| torchvision   | 0.17.2  | Компьютерное зрение                    | `pip install torchvision`    |
| torchaudio    | 2.2.2   | Обработка аудиофайлов                  | `pip install torchaudio`     |
| transformers  | 4.39.3  | Работа с языковыми моделями            | `pip install transformers`   |
| sentencepiece | 0.2.0   | Токенизация текста                     | `pip install sentencepiece`  |

## 6. Зависимости для работы с браузером
| Библиотека      | Версия | Назначение                         | Установка                     |
|----------------|--------|-----------------------------------|-------------------------------|
| selenium       | 4.18.1 | Автоматизация браузера             | `pip install selenium`       |
| playwright     | 1.43.0 | Контроль браузеров и парсинг данных | `pip install playwright`     |
| beautifulsoup4 | 4.12.3 | Извлечение данных из HTML          | `pip install beautifulsoup4` |
| lxml           | 5.1.0  | Обработка XML и HTML               | `pip install lxml`           |

## 7. Зависимости для работы с базами данных
| Библиотека   | Версия | Назначение            | Установка                |
|-------------|--------|----------------------|--------------------------|
| sqlalchemy  | 2.0.30 | ORM для работы с БД  | `pip install sqlalchemy` |
| sqlite      | 3.42.0 | Работа с SQLite      | `pip install sqlite`     |
| psycopg2    | 2.9.9  | Подключение к PostgreSQL | `pip install psycopg2`  |

## 8. Установка зависимостей
```sh
pip install -r requirements.txt
```

## 9. Обновление зависимостей
```sh
pip freeze > requirements.txt
```

# 📚 Документация

## 1. Введение
Локальный AI-оркестратор "Совет Элронда" предназначен для управления несколькими нейросетями, объединения их ответов для более широкого анализа, автоматического создания шаблонов в "Команды" macOS и интеграции с браузером.

## 2. Установка и настройка окружения
### Установка Python и виртуального окружения
Проект требует Python 3.10+. Установите его, если необходимо:
```sh
brew install python@3.10  # Для macOS
sudo apt install python3.10 python3.10-venv -y  # Для Linux
```
Затем создайте и активируйте виртуальное окружение:
```sh
python3.10 -m venv venv
source venv/bin/activate  # Для macOS/Linux
venv\Scripts\activate     # Для Windows
```

## 3. Настройки окружения
Перед запуском проекта настройте файл `.env` с API-ключами и параметрами:
```ini
OPENAI_API_KEY="sk-XXX"
LOCAL_MODEL_PATH="models/gpt4all-model.bin"
AI_MODE="hybrid"
DATABASE_URL="sqlite:///data/database.db"
SECRET_KEY="your-secret-key"
```
Для безопасности файл `.env` не должен попадать в репозиторий:
```sh
chmod 600 .env
```

## 3. Конфигурация
Настройте файл `.env` (пример приведен выше). Если используете API, убедитесь, что ключи корректны.

## 4. Установка
1. Клонируйте репозиторий:
   ```sh
   git clone git@github.com:Tarnov/Council_of_Elrond.git
   cd Council_of_Elrond
   ```
2. Установите зависимости:
   ```sh
   pip install -r requirements.txt
   ```

## 5. Запуск
```sh
python src/main.py
```

## 6. Архитектура
### Уровни системы:
1. API (FastAPI) — маршрутизация запросов и обработка ошибок.
2. Логический уровень (AI Engine) — выбор между OpenAI API и локальной моделью.
3. Инфраструктурный уровень — кеширование, хранение логов.

```mermaid
graph TD;
    Client -->|Запрос| API(FastAPI);
    API -->|Перенаправляет| AIEngine;
    AIEngine -->|Если онлайн| OpenAI API;
    AIEngine -->|Если оффлайн| GPT4All;
    AIEngine -->|Логирует| Storage(Локальное хранилище);
    API -->|Ответ| Client;
```

## 7. Основные возможности
- Оркестрация нескольких нейросетей
- Интеграция с браузером
- Автоматизация "Команд" в macOS
- Работа в оффлайн-режиме

## 8. Интеграция с браузером
- Использование **Playwright, Selenium, AppleScript** для автоматизации веб-задач
- Запись и выполнение пользовательских сценариев взаимодействия

## 9. Работа с моделями
- Поддержка локальных моделей **GPT4All, LLaMA, Mistral**
- Выбор моделей по умолчанию и их конфигурация
- Подключение дополнительных моделей через API

## 10. Логирование и мониторинг
Логи сохраняются в файле `logs/app.log`. Включение детализированного логирования:
```sh
export LOG_LEVEL=DEBUG
```

## 11. Описание API
Проект использует FastAPI для взаимодействия с клиентами. Автоматически генерируемая документация доступна по адресу:
```sh
http://127.0.0.1:8000/docs
```

## 12. Автоматизация с "Командами"
- Использование **AppleScript, Shortcuts CLI**
- Создание пользовательских автоматизированных сценариев

## 13. Безопасность
- Локальная обработка данных
- Минимальное использование внешних API
- Возможность работы полностью в оффлайн-режиме

## 14. Примеры использования
### Запрос генерации текста
```sh
curl -X POST "http://127.0.0.1:8000/orchestrator/generate" \
     -H "Content-Type: application/json" \
     -d '{"prompt": "Расскажи интересный факт о Средиземье"}'
```
### Запуск сервера
```sh
PYTHONPATH=$(pwd) python3 src/main.py
```

## 15. CI/CD и DevOps
Для автоформатирования кода:
```sh
pre-commit run --all-files
```
Для запуска тестов:
```sh
pytest tests/
```

## 15. Запуск GUI

Помимо API, проект поддерживает графический интерфейс для тестирования.

### Установка зависимостей:
```sh
pip install PyQt6 requests
```

### Запуск приложения:
1. Убедитесь, что API запущен:
   ```sh
   PYTHONPATH=$(pwd) uvicorn src.main:app --reload
   ```
2. В другом терминале запустите GUI:
   ```sh
   python app.py
   ```

Теперь можно вводить запросы и получать ответы от AI-оркестратора в удобном интерфейсе.

## 16. Заключение
AI-оркестратор "Совет Элронда" создан для удобного управления нейросетями, автоматизации задач и работы в оффлайн-режиме, предоставляя интуитивный интерфейс и гибкую настройку.
